{
  "name": "template-method",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node src",
    "start:dev": "node --watch src",
    "test": "NODE_OPTIONS='--experimental-vm-modules' npx jest -i -c jest.config.js",
    "test:dev": "NODE_OPTIONS='--experimental-vm-modules' npx jest -i --watch -c jest.config.js",
    "test:cov": "NODE_OPTIONS='--experimental-vm-modules' npx jest -c jest.config.js --coverage"
  },
  "engines": {
    "node": "20"
  },
  "keywords": [],
  "author": "antony-gp",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
